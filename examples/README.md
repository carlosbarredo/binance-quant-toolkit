# Examples

- `data/btcusdt_1h_sample.csv` is synthetic. It supports offline learning and tests.
- `download_research_bundle.py` shows how to align several REST datasets.
- `rebuild_order_book.py` demonstrates sequence-aware depth updates without a network call.

Generated market data belongs under `data/`, which Git ignores by default.

